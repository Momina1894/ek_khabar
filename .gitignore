# Collected data. Lives as an asset on the "data" release, not in git history -
# committing a growing binary every 30 minutes bloated the repo.
#   gh release download data --pattern headlines.db
headlines.db
headlines-*.db

# Build output, regenerated by build.py on every run.
site/

__pycache__/
*.pyc
